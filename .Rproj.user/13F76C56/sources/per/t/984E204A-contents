---
title: "Week04"
author: "scurry"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: TRUE
    theme: darkly
    number_sections: TRUE
    toc_float: TRUE
    toc_depth: 2
    code_folding: hide
bibliography: references.bib
    
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Introduction

## Subsection 1

## Subsection 2

### Ordered List
1. First Item
2. Second Item
3. Third Item

### Unordered List
+ Bullet Point 1
+ Bullet Point 2
+ Bullet Point 3

Today I am so excited because we're going to make **graphs** and I *love* graphs.
I also just read an amazing article about mapping neighborhood change [@delmelle2016mapping].

```{r, echo = TRUE, eval = TRUE, warning = FALSE, message = FALSE}

library(gapminder)

head(gapminder)

```

```{r}


library(ggplot2)

a <- ggplot(data = gapminder, mapping = aes(x = gdpPercap, y = lifeExp))
a + geom_point()

```

```{r message=FALSE, warning=FALSE}

a + geom_point() + geom_smooth()

a + geom_point() + geom_smooth(method = lm)
```

```{r message=FALSE, warning=FALSE}

a + geom_point()+
  geom_smooth(method = "gam")+
  scale_x_log10()

a + geom_point()+
  geom_smooth(method = "gam")+
  scale_x_log10(labels = scales::comma)

```

```{r message=FALSE, warning=FALSE}

b <- ggplot(data = gapminder,
            mapping = aes(x = gdpPercap,
                          y = lifeExp,
                          color = "purple"))
b + geom_point() +
    geom_smooth(method = "loess") +
    scale_x_log10()

c <- ggplot(data = gapminder,
            mapping = aes(x = gdpPercap,
                          y = lifeExp))
c + geom_point(color = "purple") +
    geom_smooth(method = "loess") +
    scale_x_log10()

c + geom_point(aes(color = gapminder$continent)) +
  geom_smooth(method  = "loess") +
  scale_x_log10()
```

```{r}

d <- c + geom_point(aes(color = gapminder$continent)) +
  geom_smooth(method  = "loess") +
  scale_x_log10()

d + geom_point(alpha = 0.3) +
  geom_smooth(color = "orange", se = FALSE, linewidth = 8, method = "lm")+
    scale_x_log10()
```

```{r}
d + geom_point(alpha = 0.3) +
  geom_smooth(method = "gam") +
  scale_x_log10(labels = scales::dollar)+
  labs(x = "GDP Per Capita", y = "Life Expectancy in Years",
       title = "Economic Growth and Life Expectancy",
       subtitle = "Data points are country-years",
       caption = "Source: Gapmider")
```

```{r}
e <- ggplot(data = gapminder,
            mapping = aes(x = gdpPercap,
                          y = lifeExp,
                          color = continent))
e + geom_point() +
    geom_smooth(method = "loess") +
    scale_x_log10()
ggsave(filename = "my_figure.png")
```


### References